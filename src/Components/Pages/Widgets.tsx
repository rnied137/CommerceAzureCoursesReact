import React from 'react'
import { MarkdownGenerator } from '../../FunctionalComponents/MarkdownGenerator'

export const Widgets = () => {
    return (
        <div>
            Widgets
            {/* <MarkdownGenerator source=""/> */}
        </div>
    )
}
